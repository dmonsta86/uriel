# Third-party notices

Uriel's runtime package has **no third-party runtime dependencies** and does not vendor third-party libraries.

Development and release workflows may use optional tools such as `pytest`, `build`, GitHub Actions, OpenCode, or an external/local language model. Those tools are not part of Uriel's trusted deterministic core and remain subject to their own licenses, terms, privacy policies, and security properties.
