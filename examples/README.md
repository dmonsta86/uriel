# Examples

- `rough-question.sh` demonstrates zero-cost intake and an expected constructive refusal.
- `passing_fixture.py` creates the bounded software fixture used by the tests and earns a local Blessing.

A passing example demonstrates implemented policy behavior; it is not evidence that an unrelated scientific project should pass.
